commands:
  check: /opt/resource/bin/credstash_smuggler_wrapper.sh
  in: /opt/resource/bin/credstash_smuggler_wrapper.sh ${SMUGGLER_DESTINATION_DIR}
  out: /opt/resource/bin/credstash_smuggler_wrapper.sh ${SMUGGLER_SOURCES_DIR}
